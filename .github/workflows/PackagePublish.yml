name: Package Create & Publish

on:
  push:
    branches: [ "master" ]
  
  workflow_dispatch:
    inputs:
      logLevel:
        description: "Log level"
        required: true
        default: "warning"
        type: choice
        options:
          - info
          - warning
          - debug

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      DOTNET_ROLL_FORWARD: Major

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET Core
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Smoke test
        run: bash scripts/smoke.sh

      - name: Build Release packages
        run: dotnet build Peachpie.Avalonia.sln --configuration Release

      - name: Publish NuGet Package
        run: dotnet nuget push .nuget/*.nupkg --api-key ${{ secrets.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
